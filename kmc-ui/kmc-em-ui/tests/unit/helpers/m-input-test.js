import { mInput } from 'kmc-em-ui/helpers/m-input';
import { module, test } from 'qunit';

module('Unit | Helper | m input');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mInput([42]);
  assert.ok(result);
});
